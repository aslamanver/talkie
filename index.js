/**
 * @format
 */

import { AppRegistry } from 'react-native';
import { WebRTCApp } from './src/WebRTCApp';
import { name as appName } from './app.json';

AppRegistry.registerComponent(appName, () => WebRTCApp);
